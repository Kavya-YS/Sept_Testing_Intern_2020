"""Get the input number
determine the range of the number
While iterating through the range of number square the number
print the squared number of each iteration
"""

#Fetch the number through user input
num=int(input("Input number is: "))

#Determine the range of "num"
num_list=range(num)

#iterate through the range of "num"
for x+1 in num_list:
#Find the square of each iterated num and print the value    
    print(x*x)
    